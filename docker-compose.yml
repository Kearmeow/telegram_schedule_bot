services:
  schedule-bot:
    build: .
    container_name: telegram-schedule-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
